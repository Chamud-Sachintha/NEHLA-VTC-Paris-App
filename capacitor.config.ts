import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'NEHLA-VTC-Paris-App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
